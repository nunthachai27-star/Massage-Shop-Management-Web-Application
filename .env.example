# ── Docker mode (PostgreSQL in Docker) ──
# Just set these and run: docker compose up
POSTGRES_PASSWORD=Nunthachai@461463113
JWT_SECRET=your-jwt-secret-key
NEXT_PUBLIC_API_URL=/api

# ── Supabase mode (cloud database) ──
# Uncomment these if using Supabase instead of Docker PostgreSQL
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Local development (without Docker) ──
# DATABASE_URL=postgresql://postgres:Nunthachai%40461463113@localhost:5432/massage_shop
